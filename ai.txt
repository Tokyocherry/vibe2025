Существует проект «todolist» на чистом Node.js, где index.js — единственный серверный файл. В MySQL уже есть база `todolist` и таблица `items(id, text)`. Также доступы к базе (host, user, password, database) в index.js.

Нужно реализовать **добавление задачи**:
1. В index.js создай маршрут `POST /add-item`.
2. В index.html добавь кнопку «Add» и поле ввода. По клику выполняется /add-item по методу post запроса
3. Функции работы с БД и обработчик запроса должны быть прописаны **прямо в index.js**, без отдельных файлов.
4. Покажи полный патч для index.js (добавленная функция addListItem, новый обработчик в `handleRequest`)